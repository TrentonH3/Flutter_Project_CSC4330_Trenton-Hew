# Do Nothing Button

A simple Flutter application featuring a single button that, when pressed, does absolutely nothing.

## Getting Started

1. Ensure you have the Flutter SDK installed.
2. Run `flutter pub get` to fetch dependencies.
3. Run `flutter run` to launch the app.
